(function() {
    /**
     * Create the global namespace for the game engine.
     *
     * @type {object}
     */
    window.Opus = window.Opus || {};

    /**
     * Engine version.
     *
     * @type {string}
     */
    Opus.VERSION = '0.1.0';
})();
